---
import { siteConfig } from '../site.config';

interface Props {
  currentPath: string;
}
const { currentPath } = Astro.props;

const links = [
  { href: '/', label: '首页' },
  { href: '/blog', label: '文章' },
  { href: '/progress', label: '学习进度' },
  { href: '/resources', label: '软件资源' },
  { href: '/search', label: '搜索' },
];

function isActive(href: string, path: string): boolean {
  if (href === '/') return path === '/';
  return path.startsWith(href);
}
---

<header class="site-header">
  <div class="container header-inner">
    <a href="/" class="logo">
      <span>{siteConfig.shortName[0]}</span>{siteConfig.shortName.slice(1)}
    </a>
    <nav class="nav">
      {
        links.map((l) => (
          <a
            href={l.href}
            class:list={[{ active: isActive(l.href, currentPath) }]}
          >
            {l.label}
          </a>
        ))
      }
      <button class="icon-btn" data-theme-toggle title="切换深浅色" type="button">
        🌓
      </button>
    </nav>
  </div>
</header>
